PROJECT(CppCommon)
CMAKE_MINIMUM_REQUIRED(VERSION 3.20)
SET(CMAKE_CXX_STANDARD 20)
ADD_DEFINITIONS(-Wall -g)

INCLUDE_DIRECTORIES(include)

SET(SRCS)
AUX_SOURCE_DIRECTORY(src SRCS)
AUX_SOURCE_DIRECTORY(src/file SRCS)

ADD_LIBRARY(cpp_common ${SRCS})